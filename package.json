{
  "name": "witness",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=24.15 <25"
  },
  "packageManager": "npm@11.12.1",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && vite build",
    "check:acl": "node scripts/check-tauri-acl.mjs",
    "check": "npm run build && npm run check:acl",
    "audit": "npm audit --audit-level=high",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "preact": "^10"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2",
    "@tauri-apps/cli": "^2",
    "typescript": "~5.9",
    "vite": "^7"
  }
}
