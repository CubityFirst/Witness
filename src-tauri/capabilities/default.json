{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "captions"],
  "permissions": [
    "core:default",
    "notification:default",
    "dialog:default"
  ]
}
